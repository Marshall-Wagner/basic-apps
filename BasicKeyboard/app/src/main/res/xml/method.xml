<?xml version="1.0" encoding="utf-8"?>
<!-- Declares the IME and its language subtypes. Each subtype shows up as a language
     you can switch between via the globe key / system language switcher. CJK subtypes
     can be added here in Phase 2. -->
<input-method xmlns:android="http://schemas.android.com/apk/res/android"
    android:settingsActivity="dev.montb.basickeyboard.ui.MainActivity">

    <subtype
        android:label="@string/subtype_english"
        android:imeSubtypeLocale="en_US"
        android:imeSubtypeMode="keyboard"
        android:imeSubtypeExtraValue="layout=qwerty" />

    <subtype
        android:label="@string/subtype_russian"
        android:imeSubtypeLocale="ru_RU"
        android:imeSubtypeMode="keyboard"
        android:imeSubtypeExtraValue="layout=russian" />

</input-method>
